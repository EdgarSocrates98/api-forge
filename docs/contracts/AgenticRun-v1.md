# AgenticRun/v1

Envelope for one bounded runtime execution bound to a TaskSpec revision. It records lifecycle state, invocation and artifact references, evidence, gaps, final status, replay digest and an explicit `evidence_level`. It does not replace TaskSpec acceptance or independent verification.
