# `DevinCheck/v1`

An immutable proposed verification command in a Devin payload. `command` is
declarative: generating it does not execute the command or prove its result.
`evidence_level` defaults to `declared`.
