# `DevinCliProbe/v1`

Read-only local observation of the `devin` executable. `installed` and
`cli_version` are observed only when the local `devin --version` probe completes;
account, organization, Desktop and Cloud availability remain unresolved.
