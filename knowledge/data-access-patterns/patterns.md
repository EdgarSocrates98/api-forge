<!-- generated by scripts/gen_pack_docs.py from catalog + pack data -->
# data-access-patterns

Cache-aside/read-through/write-through, TTL and invalidation, N+1, bounded reads and idempotent writes - the pattern vocabulary the data.* facts classify into.

## Extraction patterns

Verbs that produce facts for this domain:

- `model dynamodb-access`
- `model elasticache-access`
- `model neptune-access`
