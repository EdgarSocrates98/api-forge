# Runtime Migration Quick Reference

- Resolve source and target through `matrix.yaml`.
- Record missing toolchains as gaps.
- Run changes only in sandbox/worktree.
- Require an independent verifier before `DONE`.
