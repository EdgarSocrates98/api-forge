# API Forge host adapter

This project uses the installed API Forge package as its source of truth.

- Run `apiforge inspect` and `apiforge context` from the consumer project.
- Host activation is plan-only and never overwrites this file automatically.
- Optional host capabilities remain explicit when unavailable.

Generated by API Forge ${generator}; source hash: ${source_hash}.
