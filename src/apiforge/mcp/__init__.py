"""MCP surface for API Forge — thin wrappers over application services.

Tools compose and project; business logic stays in ``apiforge.application``
and friends. The ``mcp`` SDK is optional (``apiforge[mcp]``); the wrapper
layer in ``tools.py`` is importable without it.
"""
