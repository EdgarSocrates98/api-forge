# Real anonymized evidence

This directory is reserved for a provider export with repository names, URLs,
tokens, user identities and signed query strings removed. The current feature
ships with a deterministic replay bundle; a live GitHub receipt remains an
explicit unresolved evidence item until a read-only collection is authorized.
