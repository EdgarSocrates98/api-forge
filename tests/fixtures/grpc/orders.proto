syntax = "proto3";
package orders.v1;

message OrderRequest {
  string order_id = 1;
}

message Order {
  string order_id = 1;
  string status = 2;
}

service Orders {
  rpc GetOrder(OrderRequest) returns (Order);
  rpc WatchOrders(stream OrderRequest) returns (stream Order);
}
