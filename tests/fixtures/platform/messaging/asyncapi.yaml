asyncapi: 3.0.0
info:
  title: Order Events
  version: 1.0.0
channels:
  orders:
    address: orders.created
    messages:
      orderCreated:
        payload:
          type: object
          required: [order_id]
